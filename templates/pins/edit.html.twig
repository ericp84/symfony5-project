{% extends 'layouts/base.html.twig' %}

{% block title 'edit pin #' ~ pin.id %}

{% block body %}
<div class="row">
    <div class="col-md-6 mx-auto">
        <h1>edit Pin {{pin.title}}</h1>
        {{include('pins/_form.html.twig', {'submitButtonText': 'Update Pin'})}}
    </div>
</div>
{% endblock %}