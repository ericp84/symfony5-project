{% extends 'layouts/base.html.twig' %}

{% block title %}Connexion{% endblock %}

{% block body %}
    {% if error %}
        <div class="alert alert-error rounded mt-3" role="alert">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}
    <div class="row">
        <div class="col-md-6 mx-auto">
            <h1 class="text-center mt-5 mb-3">Connectez vous !</h1>
            <p class="text-center text-muted mb-5"> Vos pins vous attendent 😊</p>
            <form method="post">
                {# {% if app.user %}
                    <div class="mb-3">
                        Vous êtes connecté en tant que  {{ app.user.fullName }}, <a href="{{ path('app_logout') }}">Logout</a>
                    </div>
                {% endif %} #}
                <div class="form-group">
                    <label for="email">Email</label>
                    <input type="email" value="{{ last_username }}" name="email" id="email" class="form-control" autocomplete="email" required autofocus>
                </div>
                <div class="form-group">
                    <label for="password">Mot de passe</label>
                    <input type="password" name="password" id="password" class="form-control" autocomplete="current-password" required>
                </div>
                <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
                    {# Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
                    See https://symfony.com/doc/current/security/remember_me.html #}

                <div class="d-flex justify-content-between align-items-center">
                    <div class="form-group mb-3 mt-3">
                        <div class="form-check">
                            <input type="checkbox" class="form-check-input" name="remember_me" id="remember_me">
                            <label for="remember_me" class="form-check-label">Souvenez vous de moi</label>
                        </div>
                    </div>
                    <a href="#">Mot de passe oublié ?</a>
                </div>

            
                <div class="d-grid gap-2">
                    <button class="btn btn-lg btn-primary mt-3" type="submit">
                        Connexion
                    </button>
                </div>
                <p class="mt-5 mb-3 text-center text-muted">Pas encore de compte ? <a href="#">Cliquez ici !</a></p>
            </form>
        </div>
    </div>
{% endblock %}
